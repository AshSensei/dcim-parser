[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "dcim-parser"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["comtypes>=1.4.10"]

[project.scripts]
dcim = "dcim.cli:main"
